import { FlightPointsPipe } from './flight-points.pipe';

describe('FlightPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
